@{ 
    Layout = "/Views/Shared/_Layout.cshtml";
}

    <div class="d-flex align-items-center text-center align-content-center row row-margins">
        <h1>Manage your account</h1>
        <hr />
        <div class="col-md-3">
            @await Html.PartialAsync("_ManageNav")
        </div>
        <div class="col-md-9">
            @RenderBody()
        </div>
    </div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}
        
